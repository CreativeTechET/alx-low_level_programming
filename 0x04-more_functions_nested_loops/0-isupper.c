#include "main.h"
#include <ctype.h>

/*
 * _isupper - converts a chatr into uppercase char
 *
 * Return: the converted char
 */

int _isupper(int c)
{
	char context = isupper(c);

	return (context);
}
