#include "main.h"

/**
 * mul - function multiplies the  numbers.
 * @a: first number
 * @b: secound number
 * Return: the muliplied number
 */

int mul(int a, int b)
{
	int times = a * b;

	return (times);
}

