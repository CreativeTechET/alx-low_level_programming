#include "main.h"

/**
 * mul - multiply the a and b numbers
 * @a: first number
 * @b: secound number
 * Return: the result number
 */

int mul(int a, int b)
{
	int times = a * b;

	return (times);
}
