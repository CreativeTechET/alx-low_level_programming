#include <string.h>
#include <stdio.h>
#include <stdlib.h>


/**
 * _atoi - main function
 *
 * Return: the number
 */

int _atoi(char *s)
{
	int i;

	for (i = 0; s[i]!='\0'; i++)
	{
		int j;

		if (isdigit(s[i]))
		{

		j = 

	return (n);
}
