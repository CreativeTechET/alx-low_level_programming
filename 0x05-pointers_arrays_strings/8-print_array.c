#include <stdio.h>
#include <string.h>

/**
 * print_array - prints out the arrays n number
 * @a: number  1
 * @n: number 2
 */

void print_array(int *a, int n)
{
	int i;
	
	if (a[i]=!'\0')
	{
		for (i = 0; a[i]!='\0' ; i++)
		{
			printf("%i, ",a[n]);
		}
	}
	else
	{
		printf("%i",a[i]);
	}
}
