#include "main.h"

/**
 * reverse_array - function to reverse array
 * @a: array
 * @n: length of the array
 **/

void reverse_array(int *a, int n)
{
	int i;

	for (i = n; i <= n; i--)
	{
		printf("%d", a[i]);
	}
}
