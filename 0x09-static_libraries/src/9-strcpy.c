#include <stdio.h>
#include <string.h>

/**
 * _strcpy - main function
 * @dest: destination
 * @src: source file
 * Return: char destination
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
